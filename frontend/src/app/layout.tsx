import type { Metadata } from 'next';
import { Geist, Geist_Mono } from 'next/font/google';
import './globals.css';
import { Toaster } from "@/components/ui/toaster"; // Import Toaster

const geistSans = Geist({
  variable: '--font-geist-sans',
  subsets: ['latin'],
});

const geistMono = Geist_Mono({
  variable: '--font-geist-mono',
  subsets: ['latin'],
});

// SEO Metadata
export const metadata: Metadata = {
  title: 'Find General Physicians & Internal Medicine Doctors | Apollo 24/7 Clone',
  description: 'Book appointments online with top General Physicians & Internal Medicine specialists. Find doctors near you, check availability, and view profiles on Apollo 24/7 Clone.',
  // Add more relevant keywords and potentially Open Graph tags later
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${geistSans.variable} ${geistMono.variable} antialiased`}>
        {children}
        <Toaster /> {/* Add Toaster component here */}
      </body>
    </html>
  );
}
